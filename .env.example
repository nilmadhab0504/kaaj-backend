# Copy to .env and adjust
DEBUG=false
# Switch database by uncommenting ONE of the following:
# SQLite (default, no extra setup):
DATABASE_URL=sqlite+aiosqlite:///./lender_match.db
# PostgreSQL (uncomment to use instead):
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/lender_match
CORS_ORIGINS=http://localhost:3000

# LLM for PDF parsing (optional - improves extraction accuracy)
# OpenAI: https://platform.openai.com/api-keys | Gemini: https://aistudio.google.com/apikey
# OPENAI_API_KEY=
# GEMINI_API_KEY=
